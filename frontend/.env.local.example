# Copy to .env.local. Both vars are optional; defaults shown.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
# Set to "1" to use the in-browser scripted fake backend instead of FastAPI.
NEXT_PUBLIC_USE_FAKE_BACKEND=0
